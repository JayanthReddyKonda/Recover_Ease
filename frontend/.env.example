# ─── Frontend Environment ─────────────────────────────
# When served behind nginx (Docker), these are baked at build time.
# /api and empty SOCKET_URL let nginx proxy everything.
VITE_API_URL=/api
VITE_SOCKET_URL=
